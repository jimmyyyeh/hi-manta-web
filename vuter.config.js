module.exports = {
  projects: [
    {
      root: './hi-manta-web', // root of your vue project (should contain package.json)
      package: './hi-manta-web/package.json', // Relative to root property, don't change this.
      tsconfig: './hi-manta-web/tsconfig.json',  // Relative to root property, don't change this.
    }
  ]
}
